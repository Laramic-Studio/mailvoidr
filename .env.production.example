# Production build — copy to .env.production before `npm run build`
# Vite bakes these into the static bundle at build time.

VITE_API_URL=https://api.mailvoidr.com/api/v1
VITE_WS_URL=https://app.mailvoidr.com
VITE_BYPASS_AUTH=false
