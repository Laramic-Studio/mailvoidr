# Laravel API base URL (no trailing slash)
VITE_API_URL=http://localhost:8000/api/v1

# Allow dashboard access without JWT during local UI development (Module 1 removes need for this)
VITE_BYPASS_AUTH=true
